{
  "file_path": "utils.py",
  "content": "\"\"\"Utility functions for string manipulation and text processing.\n\nThis module provides common string helper functions for text processing,\nincluding URL slug generation, word capitalization, and text truncation.\n\"\"\"\n\nimport re\nfrom typing import Optional\n\n\ndef slugify(text: str, max_length: Optional[int] = None) -> str:\n    \"\"\"Convert a string to a URL-friendly slug.\n    \n    Removes special characters, converts to lowercase, and replaces\n    spaces and other separators with hyphens. Multiple consecutive\n    hyphens are collapsed to a single hyphen.\n    \n    Args:\n        text: The input string to convert to a slug.\n        max_length: Optional maximum length for the resulting slug.\n                   If provided, the slug will be truncated to this length.\n    \n    Returns:\n        A URL-friendly slug string containing only lowercase letters,\n        numbers, and hyphens.\n    \n    Examples:\n        >>> slugify(\"Hello World!\")\n        'hello-world'\n        >>> slugify(\"My Awesome Blog Post #1\")\n        'my-awesome-blog-post-1'\n        >>> slugify(\"Special chars: @#$%^&*()\")\n        'special-chars'\n        >>> slugify(\"Long title here\", max_length=10)\n        'long-title'\n    \"\"\"\n    if not text:\n        return \"\"\n    \n    # Convert to lowercase\n    slug = text.lower()\n    \n    # Replace spaces and underscores with hyphens\n    slug = re.sub(r'[\\s_]+', '-', slug)\n    \n    # Remove all non-alphanumeric characters except hyphens\n    slug = re.sub(r'[^a-z0-9-]', '', slug)\n    \n    # Replace multiple consecutive hyphens with a single hyphen\n    slug = re.sub(r'-+', '-', slug)\n    \n    # Remove leading and trailing hyphens\n    slug = slug.strip('-')\n    \n    # Truncate if max_length is specified\n    if max_length is not None and len(slug) > max_length:\n        slug = slug[:max_length].rstrip('-')\n    \n    return slug\n\n\ndef capitalize_words(text: str, ignore_articles: bool = False) -> str:\n    \"\"\"Capitalize the first letter of each word in a string.\n    \n    Properly handles contractions, hyphenated words, and optionally\n    ignores common articles (a, an, the, and, or, but, in, on, at, to, for, of, with).\n    \n    Args:\n        text: The input string to capitalize.\n        ignore_articles: If True, common articles and prepositions will not\n                        be capitalized unless they are the first word.\n    \n    Returns:\n        The input string with each word capitalized according to the rules.\n    \n    Examples:\n        >>> capitalize_words(\"hello world\")\n        'Hello World'\n        >>> capitalize_words(\"it's a beautiful day\")\n        \"It's A Beautiful Day\"\n        >>> capitalize_words(\"it's a beautiful day\", ignore_articles=True)\n        \"It's a Beautiful Day\"\n        >>> capitalize_words(\"well-known author\")\n        'Well-Known Author'\n    \"\"\"\n    if not text:\n        return \"\"\n    \n    # Articles and prepositions to ignore when ignore_articles is True\n    articles = {'a', 'an', 'the', 'and', 'or', 'but', 'in', 'on', 'at', 'to', 'for', 'of', 'with'}\n    \n    words = text.split()\n    capitalized_words = []\n    \n    for i, word in enumerate(words):\n        # Handle hyphenated words\n        if '-' in word:\n            parts = word.split('-')\n            capitalized_parts = []\n            for j, part in enumerate(parts):\n                if part:\n                    if ignore_articles and i > 0 and j > 0 and part.lower() in articles:\n                        capitalized_parts.append(part.lower())\n                    else:\n                        capitalized_parts.append(part.capitalize())\n                else:\n                    capitalized_parts.append(part)\n            capitalized_words.append('-'.join(capitalized_parts))\n        else:\n            # Check if we should ignore this article\n            if ignore_articles and i > 0 and word.lower() in articles:\n                capitalized_words.append(word.lower())\n            else:\n                capitalized_words.append(word.capitalize())\n    \n    return ' '.join(capitalized_words)\n\n\ndef truncate_text(text: str, max_length: int, ellipsis: str = \"...\", \n                  preserve_words: bool = True) -> str:\n    \"\"\"Truncate text to a specified maximum length.\n    \n    Safely truncates text while optionally preserving word boundaries\n    and adding an ellipsis indicator.\n    \n    Args:\n        text: The input string to truncate.\n        max_length: The maximum length of the resulting string, including ellipsis.\n        ellipsis: The string to append when text is truncated. Defaults to \"...\".\n        preserve_words: If True, avoid cutting words in the middle by truncating\n                       at the last complete word that fits. Defaults to True.\n    \n    Returns:\n        The truncated string, with ellipsis appended if truncation occurred.\n    \n    Examples:\n        >>> truncate_text(\"This is a long sentence\", 10)\n        'This is...'\n        >>> truncate_text(\"This is a long sentence\", 10, preserve_words=False)\n        'This is...'\n        >>> truncate_text(\"Short\", 10)\n        'Short'\n        >>> truncate_text(\"This is a long sentence\", 15, ellipsis=\" [more]\")\n        'This is [more]'\n    \"\"\"\n    if not text:\n        return \"\"\n    \n    if len(text) <= max_length:\n        return text\n    \n    if max_length <= len(ellipsis):\n        # If max_length is too small to fit ellipsis, return truncated ellipsis\n        return ellipsis[:max_length]\n    \n    # Calculate available space for actual text\n    available_length = max_length - len(ellipsis)\n    \n    if not preserve_words:\n        return text[:available_length] + ellipsis\n    \n    # Find the last space within the available length\n    truncated = text[:available_length]\n    last_space = truncated.rfind(' ')\n    \n    if last_space == -1:\n        # No spaces found, truncate at character boundary\n        return text[:available_length] + ellipsis\n    \n    # Truncate at the last complete word\n    return text[:last_space] + ellipsis\n"
  "explanation": "Created a comprehensive utils.py module with three string helper functions: slugify() for URL-friendly slug generation with regex-based cleaning and optional length limits, capitalize_words() for proper word capitalization with support for articles/prepositions and hyphenated words, and truncate_text() for safe text truncation with word boundary preservation. Each function includes detailed docstrings with parameter descriptions, return values, and usage examples, plus proper type hints for better code maintainability."
}